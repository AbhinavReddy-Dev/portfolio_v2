---
interface Navlink {
  href: string;
  id: string;
  title: string;
}
const linkProps: Navlink = Astro.props.navlink;
---

<a
  href={linkProps.href}
  id={linkProps.id}
  title={linkProps.title}
  class="mb-auto block h-fit rounded-lg border border-transparent px-4 py-1 text-gray-700 target:border target:border-black target:bg-gradient-to-bl target:from-sky-300 target:via-indigo-300 target:to-violet-300 target:text-black target:outline-none hover:border hover:border-violet-600 dark:text-gray-400 target:dark:border-white target:dark:from-sky-900 target:dark:via-indigo-900 target:dark:to-violet-900 target:dark:text-white hover:dark:border-violet-400">
  {linkProps.title}</a
>
